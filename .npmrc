# Hoist all packages to the root node_modules so tools (esbuild, tsx, etc.)
# are reachable by plain `node` ESM imports in workspace packages.
# This is the standard pnpm approach for monorepos that use build tooling.
shamefully-hoist=true
